from fastapi import APIRouter

router = APIRouter(prefix="/candidates", tags=["candidates"])

# Selección de candidaturas (docs/02-metodologia.md#7-selección-de-candidaturas).
# Pendiente de implementación: corresponde a la Etapa 6 de la hoja de ruta.
